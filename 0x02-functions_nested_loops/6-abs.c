#include "main.h"
/**
 * _abs - check the code.
 * @r: the input number intggre
 * Description: a function that computes the absolute value of     an integer
 * Return: Always 1 is greater than zero
 */
int _abs(int r)
{
	if (r >= 0)
	return (r);
	else
		return (r * -1);
}
