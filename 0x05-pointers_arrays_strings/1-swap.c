#include "main.h"
/**
 * swap_int - check the code
 * @a: a is swap integer
 * @b: b is an integer
 * Description: this function swaps value of two int.
 */
void swap_int(int *a, int *b)
{
	int godwin = *a;
	*a = *b;
	*b = godwin;
}
